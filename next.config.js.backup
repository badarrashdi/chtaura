/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
    esmExternals: true,
  },
  transpilePackages: ['lucide-react'],
  images: {
    domains: [],
  },
};

module.exports = nextConfig;
